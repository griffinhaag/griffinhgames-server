# GriffinGames Server

Modular multiplayer backend for GriffinGames.

- Node.js
- Express
- Socket.IO
- Modular game engine (rooms + pluggable game types)
- HTTP API deployable on Vercel; full server (with Socket.IO) runs on Fly.io, Railway, or any Node host

## Running Locally

```bash
npm install
npm start
